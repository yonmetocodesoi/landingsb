[build]
  command = "npm run build"
  publish = "out"
  functions = ".netlify/functions/"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

